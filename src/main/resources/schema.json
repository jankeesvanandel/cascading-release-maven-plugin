{
    "$schema": "http://json-schema.org/draft-04/schema#",
    "title": "Release descriptor",
    "description": "Defines the complete release package",
    "type": "object",
    "properties": {
        "name": {
            "description": "The name of the releasable composition",
            "type": "string"
        },
        "parentPath": {
            "description": "The path to the parent, measured from the base. If the base is the parent, you can enter an empty string",
            "type": "string"
        },
        "distPath": {
            "description": "The path to the releasable module from the base of the project. The base is the directory that contains all modules",
            "type": "string"
        }
    },
    "required": ["name", "parentPath", "distPath"]
}